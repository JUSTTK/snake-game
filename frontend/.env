# WebSocket 服务器地址
# 本地开发环境 — 使用相对路径经过 Vite 代理（见 vite.config.ts）
VITE_WS_URL=/ws

# 直接连接后端（如果不用 Vite 代理可以改回这个）
# VITE_WS_URL=ws://localhost:8081/ws

# API 服务器地址（可选，用于 HTTP 请求）
VITE_API_URL=http://localhost:8081